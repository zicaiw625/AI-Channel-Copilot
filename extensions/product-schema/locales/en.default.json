{
  "product_schema": {
    "name": "Product Schema (JSON-LD)",
    "settings": {
      "enable": {
        "label": "Enable Product Schema",
        "info": "Outputs JSON-LD structured data on product pages for better SEO and rich snippets in search results"
      }
    }
  }
}
