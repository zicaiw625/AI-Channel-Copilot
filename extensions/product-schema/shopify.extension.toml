# AI Channel Copilot - Product Schema Extension
# 自动为产品页面注入 JSON-LD 结构化数据，提升 AI 可发现性

api_version = "2025-01"
type = "theme"

[settings]
# 扩展名称和句柄
name = "t:name"
handle = "product-schema"

[[settings.fields]]
key = "include_reviews"
type = "single_line_text_field"

[capabilities]
# 允许在 Theme App Embed 中使用
app_embed = true
